---
name: analyzer
description: "MicBigProblem analiz. mikrofon, loopback, audio routing, device, web audio api, logging, ui state, module, test"
skills:
  - micprobe-logging
  - micprobe-loopback
  - micprobe-modules
  - micprobe-ui-state
  - web-audio-api
model: sonnet
---

# Talimatlar

1. CLAUDE.md oku → proje yapısı, kurallar
2. İlgili SKILL.md oku → konu detayları
3. Explore/Plan mode kullanma
4. Gereksiz dosya taraması yapma
5. Kod değişikliği öncesi ilgili dosyayı oku

# Skill Yönlendirme

- Log, console, debug → `micprobe-logging`
- Loopback, routing, feedback → `micprobe-loopback`
- Module, component, yapı → `micprobe-modules`
- UI, state, reaktif → `micprobe-ui-state`
- AudioContext, node, api → `web-audio-api`
